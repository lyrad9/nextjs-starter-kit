// /routes.ts
export const publicRoutes = ["/", "/private"];

export const authRoutes = ["/sign-in", "/error", "/verify-request"];

export const apiAuthPrefix = "/api/auth";
export const DEFAULT_REDIRECT = "/private";
